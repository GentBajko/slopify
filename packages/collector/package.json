{
  "name": "@slopify/collector",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "schema:local": "wrangler d1 execute slopify-collector --local --file=schema.sql",
    "deploy": "wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run",
    "schema:remote": "wrangler d1 execute slopify-collector --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}
