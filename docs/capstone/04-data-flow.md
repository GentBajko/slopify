---
generated_at_commit: 4cfe3473f74d
generated_date: '2026-09-13'
capstone_version: 5.2.0
content_hash: e0a747f481e2
paths_covered:
  - :(top)packages/app/src/**
  - :(top)packages/web/src/**
  - :(top)packages/collector/**
  - :(top)packages/site/**
absorbed_from:
  - features/2026-09-24-narration-preparation@2026-09-24
  - features/2026-09-10-editable-projects@2026-09-12
  - features/2026-09-10-play-redesign-drafts@2026-09-13
  - features/2026-09-10-review-checkpoints@2026-09-13
  - features/2026-09-10-project-templates@2026-09-13
---

# Data flow

Source snapshot: `7bdb84e3f57ec19c11e21b43cb6502a720156e9f` (2026-09-13). This chapter describes the current implementation.

## Lifecycles

Narration flow verified 2026-09-24: Markdown → plain body → logical chunks/text overrides → cue-only LLM work → bounded tagged TTS paired with exact clean substrings → audio and segment-scoped text downloads. All body preparations settle before physical ordinals materialize (`packages/app/src/slices/rebuild/recipe-audio.ts:46`, `packages/app/src/slices/rebuild/recipe-preparation.ts:55`). Captions join clean source slices, never bracket-stripped scripts (`packages/app/src/slices/rebuild/runtime-narration-text.ts:18`, `packages/app/src/slices/rebuild/runtime-export-inputs.ts:85`). The broad historical snapshot is not advanced.

Checkpoint setup is saved with the reviewed draft or through the project PATCH route. Start persists gate closures and approval fingerprints with the revision. Runner claims consult the current reservation work keys; independent work proceeds while the selected closure waits. Approval commits under the project lock, emits a scoped update and wakes the runner after commit. Save/revision carry compares reviewed inputs, and restart restores held/released rows without a new provider submission.

1. **Boot and shutdown.** CLI configuration reaches `boot`; boot creates private directories, acquires the instance lock, resolves FFmpeg, opens/migrates SQLite, marks interrupted projected stages, recovers checkpoint/work state, and reconciles files. It then constructs the event hub, telemetry flusher, live catalogue/curated adapters, audio-preview store, runner, template-backed schedule runner, updater and HTTP app. The batch timer enters the updater mutation gate every second; the schedule runner recovers abandoned runs, settles terminal admitted occurrences, and ticks under the same mutation gate once at boot and every 15 seconds. Shutdown rejects new HTTP writes, gives admitted writes five seconds to finish before closing their sockets, drains the schedule tick, aborts runner work, then closes telemetry, SQLite and the lock. (`packages/app/src/edge/cli.ts:19`, `packages/app/src/main.ts:96`, `packages/app/src/main.ts:113`, `packages/app/src/main.ts:263`, `packages/app/src/main.ts:292`, `packages/app/src/main.ts:303`, `packages/app/src/main.ts:318`.)

2. **Edit → durable draft → restore/discard.** Shell mounts `PlayDraftProvider`, which owns `useDraftSession`; `PlayForm` obtains that session and the compatibility form adapter (`packages/web/src/components/shell.tsx:56`, `packages/web/src/play/draft-context.tsx:55`, `packages/web/src/routes/play.tsx:47`, `packages/web/src/lib/form-drafts.tsx:49`). A first edit immediately begins creation, while later edits debounce a flush for 500 ms; the owner serializes frozen document/version/mutation snapshots and acknowledges only the sent edit generation, retaining newer local edits (`packages/web/src/play/use-draft-session.ts:77`, `packages/web/src/play/use-draft-session.ts:106`, `packages/web/src/play/use-draft-session.ts:156`, `packages/web/src/play/draft-save.ts:32`). Draft API requests reach create/PUT handlers, whose service validates schema and references, uses SQLite savepoints, checks creation replay or last-save mutation hash, and performs a version/state compare-and-swap; saving increments version, clears persisted review identity and synchronizes attachments (`packages/web/src/play/draft-api.ts:94`, `packages/app/src/edge/http/drafts.ts:57`, `packages/app/src/edge/http/drafts.ts:69`, `packages/app/src/slices/play-drafts/service.ts:155`, `packages/app/src/slices/play-drafts/service.ts:175`). After a successful save, reference-aware cleanup releases removed attachment bytes; references still held by another draft remain (`packages/app/src/slices/play-drafts/service.ts:206`, `packages/app/src/slices/storage/staging-refs.ts:14`). Section navigation flushes, New flushes before resetting, and restore reads the selected or browser-remembered server ID and installs the returned document; active/starting drafts list by edit time, with unreadable documents flagged (`packages/web/src/play/use-draft-session.ts:186`, `packages/web/src/play/use-draft-session.ts:210`, `packages/web/src/play/use-draft-session.ts:294`, `packages/web/src/play/use-draft-session.ts:330`, `packages/app/src/slices/play-drafts/repo.ts:43`, `packages/app/src/slices/play-drafts/service.ts:146`). Fork uses an independently replayable creation ID, allocates fresh attachment IDs and shares ready staged files; missing/incomplete attachments become reattach entries (`packages/app/src/slices/play-drafts/service.ts:212`). Discard checks supplied version, blocks pending Start, deletes the draft and cascaded attachments, then releases only unshared staged files (`packages/app/src/edge/http/drafts.ts:94`, `packages/app/src/slices/play-drafts/service.ts:266`, `packages/app/src/kernel/db/migrations/0006-play-drafts.sql:18`).

Templates use the same acknowledged draft boundary. The Templates screen reads a selected draft and posts a strict reusable document; project headers post the selected current revision. The template service snapshots selected library bodies, strips credentials, paths, font operations and media ownership, then stores an immutable head/revision pair. Apply creates a fresh draft with provenance and remapped reattach references but no Review or Start receipt; the browser retains the instantiate identity until the draft opens successfully and fences delayed completion against navigation or a newer Play generation (`packages/app/src/slices/project-templates/service.ts`, `packages/app/src/slices/project-templates/setup.ts`, `packages/app/src/slices/project-templates/from-project.ts`, `packages/web/src/routes/templates.tsx`, `packages/web/src/play/use-draft-session.ts`).

Schedules use the same template boundary without browser state. The Schedules screen saves a cadence, timezone, missed/overlap policy, spend ceiling and keyword variants through `/api/schedules`. Boot recovers `running` schedule runs, then the scheduler claims due rows under a transaction, advances recurrence, creates a fresh draft, reviews its exact runs and estimate, and starts through the normal batch/provider queue. Unique schedule/occurrence keys, version compares and safe run-history errors make restart and duplicate ticks idempotent (`packages/app/src/slices/schedules/{calendar,service,scheduler,repo}.ts`, `packages/app/src/edge/http/schedules.ts`, `packages/web/src/routes/schedules.tsx`).

Portable storage flows through Settings. Export reads current settings, prompts, entries, voices, template heads and present staged files, writes a strict version-1 manifest plus raw staging entries, and returns one in-memory ZIP. Import bounds the request at 100 MiB, parses the whole ZIP/manifest, then upserts settings/libraries/voices, inserts only missing templates and copies staged bytes under new IDs. Storage usage recursively sums data, projects and staging; diagnostics independently reads provider readiness, schema/app/runtime versions, project count and catalogue status into a no-store JSON download (`packages/app/src/edge/http/storage.ts:7`, `packages/app/src/slices/storage/portable.ts:85`, `packages/app/src/slices/storage/portable.ts:135`, `packages/app/src/edge/http/diagnostics.ts:10`).

3. **File pick → owned attachment → retained restart.** File selection creates attachment UUID/name references and flushes them before sending file bytes. The browser checks draft/attachment ownership, operation generation and mounted lifetime before accepting a response (`packages/web/src/play/use-draft-uploads.ts:77`, `packages/web/src/play/use-draft-uploads.ts:110`, `packages/web/src/play/use-draft-uploads.ts:125`). The attachment PUT uses a multipart consumer; that consumer does not end until the full multipart envelope succeeds (`packages/app/src/edge/http/draft-files.ts:30`, `packages/app/src/edge/http/multipart.ts:63`). Upload service accepts only a pending, unbound attachment on an active draft, then staging inserts its copying row and binds the attachment in one allocation savepoint before opening the private disk stream (`packages/app/src/slices/play-drafts/uploads.ts:28`, `packages/app/src/slices/play-drafts/uploads.ts:38`, `packages/app/src/slices/storage/staging.ts:116`, `packages/app/src/slices/storage/staging.ts:155`). Completion marks staged bytes, then marks only the matching attachment ready and attempts reference-aware cleanup; a removed attachment cannot be recreated by completion (`packages/app/src/slices/storage/staging.ts:182`, `packages/app/src/slices/play-drafts/uploads.ts:60`). Boot reconciliation retains referenced completed files whose recorded size matches disk; interrupted/missing files detach and become reattach, orphan files are removed, and unreferenced staging rows are deleted (`packages/app/src/slices/storage/reconcile.ts:69`). GET attachment preview resolves an owned ready image/thumbnail and streams its staged bytes without caching; audio is excluded (`packages/app/src/edge/http/draft-files.ts:50`).

4. **Review → exact Start → single or batch admission → navigation.** Browser review refreshes choices/model queries, flushes pending saves, captures draft/version/edit generation, and ignores obsolete review replies (`packages/web/src/play/review-state.ts:151`). Its Review summary derives only the providers needed by active generated sources and shows current key/CLI, model and saved-voice readiness; missing items link back to the owning field. These client checks describe the fetched state and do not replace the server's Start-time checks (`packages/web/src/play/review-summary.tsx:16`, `packages/web/src/play/review-summary.tsx:110`, `packages/web/src/play/review-summary.tsx:258`). The review route reads and gates the saved draft; conversion selects active supplied IDs, resolves current templates, variants and settings, validates admission/models, estimates every run, and fingerprints resolved runs, estimates, catalogue, active attachment IDs/bytes and resolved font content (`packages/app/src/edge/http/drafts.ts:113`, `packages/app/src/slices/play-drafts/review.ts:49`, `packages/app/src/slices/play-drafts/review-inputs.ts:68`, `packages/app/src/slices/play-drafts/review-inputs.ts:137`). Font resolution is bracketed by binding checks; a final synchronous re-resolution precedes storing public review and private catalogue/attachment/font execution data, reusing an unchanged prior review ID (`packages/app/src/slices/play-drafts/review.ts:18`, `packages/app/src/slices/play-drafts/review.ts:62`). Explicit Start sends that review ID, draft ID and version; the server first replays a matching durable receipt or claims the draft as starting, then recomputes saved review identity and checks readiness asynchronously (`packages/web/src/play/review-state.ts:191`, `packages/app/src/edge/http/drafts.ts:125`, `packages/app/src/slices/play-drafts/start.ts:31`, `packages/app/src/slices/play-drafts/start.ts:74`, `packages/app/src/slices/play-drafts/start.ts:91`). Receipt replay validates all request identity fields/hash before staging resolution (`packages/app/src/slices/play-drafts/start-repo.ts:66`). The final transaction rechecks receipt, current review identity and local readiness; calls single-run `startRun` or `enqueueBatch` with staging retained; copies supplied assets, creates initial revision/admitted work; inserts Start receipt; marks the draft started; and deletes its attachment references (`packages/app/src/slices/play-drafts/start.ts:95`, `packages/app/src/slices/admission/start.ts:68`, `packages/app/src/slices/batch/index.ts:50`, `packages/app/src/slices/play-drafts/start-repo.ts:119`, `packages/app/src/slices/play-drafts/start-repo.ts:137`). After commit it records creation telemetry, releases unshared staged files and ticks the runner or pumps the batch queue; batch progression still waits for in-flight work to drain and holds paused projects (`packages/app/src/slices/play-drafts/start.ts:123`, `packages/app/src/slices/batch/index.ts:76`). Only a confirmed result clears the active browser session/ID, invalidates projects/staging/draft lists and navigates to the first project (`packages/web/src/play/use-draft-session.ts:310`, `packages/web/src/routes/play.tsx:126`, `packages/web/src/routes/play.tsx:25`). Started draft records and receipts remain on the server, while the default list excludes started drafts (`packages/app/src/slices/play-drafts/start-repo.ts:137`, `packages/app/src/slices/play-drafts/repo.ts:43`).

5. **Tutorial progress → server cursor → reload.** `useTutorialSession` queues cursor changes and serializes stable mutation IDs against a version; its API reaches GET/PUT/DELETE tutorial routes and a SQLite settings record under `tutorial.session` (`packages/web/src/tutorial/use-session.ts:33`, `packages/app/src/edge/http/tutorial.ts:13`, `packages/app/src/slices/settings/tutorial.ts:34`, `packages/app/src/slices/settings/tutorial.ts:57`). Restore validates the stored schema/step, filters retained prompt IDs against current prompts, checks a retained project, then applies deferred client updates; tutorial Play targets request the owning Play section before spotlight reveal (`packages/web/src/tutorial/use-session.ts:84`, `packages/web/src/tutorial/runner.tsx:86`). Reset deletes the record before starting a fresh queued cursor (`packages/app/src/slices/settings/tutorial.ts:80`, `packages/web/src/tutorial/use-session.ts:133`).

6. **Save and restore are separate from rebuilding.** The project workspace prepares a revision view, deep-clones its config/content into an edit, and submits a base revision plus stable idempotency UUID. Save validates intent and staged references, prepares immutable assets outside SQLite, then synchronously rechecks the base, plans recipes, inserts the new revision/assets/manifest, conditionally advances the head, transitions work, updates the current projection and records the receipt. Consumed staging metadata is deleted in that transaction only when no Play draft still references it; after commit, source bytes are released through the reference-aware cleanup helper. (`packages/app/src/slices/storage/repo.ts:93`, `packages/app/src/slices/revisions/mutations.ts:188`, `packages/app/src/slices/storage/staging-refs.ts:14`.) Restore creates a new revision with the selected historical config/manifest, current parent and historical `restoredFromId`, then transitions work and records its own receipt. Neither mutation admits newly required provider work. (`packages/web/src/project/revision-workspace.tsx:82`, `packages/web/src/project/revision-workspace.tsx:116`, `packages/app/src/slices/revisions/mutations.ts:47`, `packages/app/src/slices/revisions/mutations.ts:88`, `packages/app/src/slices/revisions/mutations.ts:159`, `packages/app/src/slices/revisions/restore.ts:23`, `packages/app/src/slices/rebuild/transition-repo.ts:1`.)

7. **Explicit rebuild review → admitted work.** Preview requires the current revision and saves the public dependency/cost review alongside the private execution snapshot. Start first checks a durable receipt, then the preview/base, exact provided-content confirmations and unknown-cost acknowledgement. Provider readiness runs before the project lock; under that lock Start rechecks receipt/head, replans against current assets/catalogue/submission state, refuses stale or more costly authorization, performs final local readiness, admits work and ticks the runner. A reviewed paid request becoming a free join remains admissible; a free join becoming a new paid submission requires a fresh preview. Runner ticks materialize only admitted work; ready supplied assets count toward stage standings without requiring a provider submission. (`packages/app/src/slices/rebuild/service.ts:30`, `packages/app/src/slices/rebuild/service.ts:46`, `packages/app/src/slices/rebuild/service.ts:79`, `packages/app/src/slices/rebuild/service.ts:140`, `packages/app/src/slices/rebuild/preview-plan.ts:1`, `packages/app/src/main.ts:378`, `packages/app/src/slices/rebuild/runtime-store.ts:1`.)

8. **Provider calls and late results.** The composition root injects revision invocation execution and the provider queue into the runner. Queue admission occurs before attempt creation; each attempt checks durable submission authority unless it is retrieving a recorded continuation. LLM events produce transient text previews and reset the idle deadline; TTS streams collect the successful complete audio while optionally forwarding preview chunks. Work executes its saved recipe and publishes an immutable output/piece bundle. Publication always targets its originating revision; it also attaches to the current revision only when that revision still owns the matching reservation and desired fingerprint. Changed in-flight results therefore remain historical, while unchanged carried work can satisfy the new head. (`packages/app/src/main.ts:345`, `packages/app/src/kernel/runner/queue.ts:12`, `packages/app/src/kernel/runner/providers.ts:86`, `packages/app/src/kernel/runner/attempt.ts:60`, `packages/app/src/slices/rebuild/runtime-run.ts:1`, `packages/app/src/slices/rebuild/runtime-publication.ts:72`, `packages/app/src/slices/rebuild/repo.ts:16`.)

9. **Pause, restart and continuation.** Revision-aware Pause claims/checks its receipt under the per-project control lock, persists the paused flag, emits an update, then aborts and drains actual in-flight work. The runner maps pause aborts to pending and prevents downstream launch during drain. At boot, interrupted projected stages become failed or pending according to the paused flag; unfinished revision work is held, except queued batch entries. Completed assets remain registered. Retry/resume of a headed project requires rebuild review. TTS continuation tokens are read/written through the attempt store for the durable work piece, so a subsequent admitted retrieval can use a known accepted job instead of submitting again; an unknown interrupted submission is represented in the rebuilt cost review. (`packages/app/src/slices/control/index.ts:101`, `packages/app/src/slices/control/index.ts:122`, `packages/app/src/kernel/runner/index.ts:150`, `packages/app/src/kernel/runner/index.ts:261`, `packages/app/src/main.ts:464`, `packages/app/src/slices/rebuild/repo.ts:93`, `packages/app/src/slices/control/index.ts:144`, `packages/app/src/kernel/runner/providers.ts:168`, `packages/app/src/kernel/runner/attempt.ts:69`, `packages/app/src/slices/rebuild/preview-retained.ts:1`.)

10. **Retained downloads and folders.** Revision URLs identify project, revision and manifest record. The resolver joins the scoped manifest to registered assets, resolves the registered path and derives the download name from that revision's saved title. Image ZIPs follow the saved selected image order and skip absent files. Download HTTP streams a complete response; folder POST uses the same resolver before opening its directory and rejects a different supplied Origin. Reconciliation retains the union of current output paths and all registered historical assets. (`packages/app/src/slices/revisions/downloads.ts:15`, `packages/app/src/slices/revisions/downloads.ts:50`, `packages/app/src/edge/http/revision-files.ts:20`, `packages/app/src/edge/http/revision-files.ts:53`, `packages/app/src/slices/storage/reconcile.ts:14`.)

11. **Catalogue, captions and telemetry.** Catalogue reads reload changed valid YAML and keep the last parsed value when local edits fail validation; explicit refresh downloads, validates, backs up and atomically replaces it. Model pickers poll every 30 seconds. Local subtitle preparation caches aligned words/font metadata, emits subtitle files and retains omission notes; revision subtitle/export recipes publish their own immutable results. Usage events enter the app's SQLite queue, the flusher sends bounded batches, and the collector deduplicates event IDs before incrementing aggregates. (`packages/app/src/catalog/store.ts:24`, `packages/web/src/lib/models.ts:28`, `packages/app/src/slices/subtitles/prepare.ts:89`, `packages/app/src/slices/rebuild/runtime-subtitles.ts:1`, `packages/app/src/slices/rebuild/runtime-export.ts:1`, `packages/app/src/slices/telemetry/flush.ts:29`, `packages/collector/src/index.ts:62`.)

12. **Retained source changes and caption edits.** Save distinguishes actual replacement assets from observations that merely fill duration metadata; inspection does not grant permission to reuse incompatible speech. Reactivating a supplied recording/thumbnail resolves its saved asset and validates availability instead of treating an unchanged dormant ID as the selected output. The caption editor uses complete current narration, including active generated entries and silence gaps, without requiring a finished export. Manual cue edits bind to that reviewed audio identity. WAV/media fingerprints exclude observational duration/transcript; caption timeline identity includes them. Generated image previews resolve current selected work-key records, while explicit supplied draft assets resolve their own retained records. (`packages/app/src/slices/revisions/mutation-prepare.ts:21`, `packages/app/src/slices/revisions/mutation-assets.ts:162`, `packages/app/src/slices/rebuild/recipe-audio.ts:24`, `packages/app/src/slices/rebuild/runtime-export-inputs.ts:39`, `packages/web/src/project/revision-caption-duration.ts:3`, `packages/web/src/project/image-preview.tsx:5`.)

13. **Partial text and dependency recovery.** Research synthesis consumes only planner/chapter payloads matching current recipe fingerprints. A cached terminal article request/continuation chain can publish locally after a previous write failure; a truncated chain still needs another provider request and readiness. Interrupted partial article text is registered as unselected historical output under its originating work authority. A remaining metadata record does not release dependent work until the full required bundle is ready. Rebuild review exposes actual before/after inputs and human request labels/text/settings. (`packages/app/src/slices/rebuild/runtime-plan.ts:127`, `packages/app/src/slices/rebuild/preview-retained.ts:116`, `packages/app/src/slices/rebuild/runtime-publication.ts:169`, `packages/app/src/slices/rebuild/runtime-store.ts:1`, `packages/app/src/slices/rebuild/preview-details.ts:9`.)

## State

| State | Owner and lifetime |
|---|---|
| Current project projection, stages, stage pieces, attempts and outputs | App SQLite; revision execution updates the current projection. `packages/app/src/kernel/db/migrations/0001-init.sql:1`, `packages/app/src/main.ts:378`. |
| Historical config/content, current head, registered assets, revision output/piece manifests, Save/Restore receipts | App SQLite revision repositories; media bytes are project-relative disk assets. `packages/app/src/kernel/db/migrations/0004-project-revisions.sql:1`, `packages/app/src/slices/storage/assets.ts:30`. |
| Work, physical pieces, reservations, accepted continuations, previews/private snapshots, admissions, provided-content reviews and control receipts | App SQLite work/attempt repositories; process restart holds unfinished dispatch. `packages/app/src/kernel/db/migrations/0005-revision-work.sql:2`, `packages/app/src/kernel/runner/providers.ts:171`, `packages/app/src/slices/rebuild/repo.ts:93`. |
| Paused flags and batch queue/order | App SQLite. `packages/app/src/kernel/db/migrations/0002-project-controls.sql:1`, `packages/app/src/kernel/db/migrations/0003-batch-queue.sql:1`. |
| Prompt/entry library, provider keys, saved voices, settings and CLI command overrides | App SQLite, owned by library/settings repositories. `packages/app/src/kernel/db/migrations/0001-init.sql:7`, `packages/app/src/slices/settings/repo.ts:1`, `packages/app/src/slices/settings/cli-paths.ts:1`. |
| Fonts, subtitle word caches and alignment model files | Private data/project directories; font uploads are written through a temporary file, subtitle metadata references cached words, alignment uses the English subtitle model cache directory. `packages/app/src/slices/fonts/upload.ts:49`, `packages/app/src/slices/subtitles/prepare.ts:56`, `packages/app/src/slices/subtitles/prepare.ts:180`. |
| Model catalogue | Private `models.yaml`, prior-file backup, and last valid in-memory catalogue/stamp/warning/refresh promise. `packages/app/src/catalog/store.ts:29`. |
| Active invocations/controllers, queued provider waiters, stopped-project set, subscribers, running tally, LLM/audio previews | Process memory owned by runner, provider queue, hub and audio store; not durable outputs. `packages/app/src/kernel/runner/index.ts:64`, `packages/app/src/kernel/runner/queue.ts:12`, `packages/app/src/edge/events/hub.ts:70`, `packages/app/src/kernel/audio-preview.ts:65`. |
| Update availability/status/check promise/mutation gate | In-memory updater service; installation plan/version pointer are files under private updates storage. `packages/app/src/updater/service.ts:21`, `packages/app/src/updater/install.ts:17`, `packages/app/src/updater/plan.ts:96`. |
| Server data cache | One React Query client; shared keys identify projects, revisions/files, staging, library, settings and usage. SSE patches or invalidates this cache. `packages/web/src/main.tsx:22`, `packages/web/src/queries.ts:16`, `packages/web/src/events.ts:58`. |
| Project revision editor state | Revision edit, base view, open history/review, pending flags and request UUID memories remain component state/refs, separate from saved Query data; remote revision changes preserve the mounted edit and show conflict. `packages/web/src/project/revision-workspace.tsx:40`, `packages/web/src/project/revision-workspace.tsx:97`. |
| Usage delivery and totals | Local telemetry/machine tables; remote collector D1 events/aggregates. `packages/app/src/kernel/db/migrations/0001-init.sql:15`, `packages/collector/src/index.ts:62`. |
| Play document/version, creation replay hash, last-save replay identity, review/public and private snapshot, starting identity | SQLite `play_drafts`, mutated by draft CRUD/review/Start services. Document contains section, form, variants, expected words and pending font-upload identity. (`packages/app/src/kernel/db/migrations/0006-play-drafts.sql:1`, `packages/app/src/slices/play-drafts/model.ts:1`, `packages/app/src/slices/play-drafts/service.ts:175`, `packages/app/src/slices/play-drafts/review.ts:86`.) |
| Attachment ownership and readiness | SQLite `play_draft_attachments`; CRUD/fork/upload/reconciliation mutate references/status. Files may have multiple draft references. (`packages/app/src/kernel/db/migrations/0006-play-drafts.sql:18`, `packages/app/src/slices/play-drafts/service.ts:252`, `packages/app/src/slices/play-drafts/uploads.ts:42`, `packages/app/src/slices/storage/reconcile.ts:69`.) |
| Start receipt | SQLite `play_start_receipts`, keyed by review/request ID and unique per draft/version, independent of draft foreign-key deletion. Start writes request hash and project/queue result atomically with admission. (`packages/app/src/kernel/db/migrations/0006-play-drafts.sql:29`, `packages/app/src/slices/play-drafts/start.ts:95`.) |
| Template heads, immutable revisions and Apply receipts | SQLite `project_templates`, `project_template_revisions` and `project_template_instantiations`; selected prompt/entry bodies are stored in the revision document, while applied drafts own remapped attachment references. (`packages/app/src/kernel/db/migrations/0008-project-templates.sql:1`, `packages/app/src/slices/project-templates/repo.ts`, `packages/app/src/slices/project-templates/service.ts`.) |
| Staging | SQLite `staged_files` and private disk; completed referenced bytes survive restart, releases defer inside any transaction or while a copy is active. (`packages/app/src/slices/storage/staging-refs.ts:14`, `packages/app/src/slices/storage/reconcile.ts:69`.) |
| Browser server-state cache | Existing shared React Query client owns query lists/choices; draft-list invalidation is `play-drafts`, choices refresh individually, uploads update fonts cache. The active draft server view is also held in session refs rather than a Query-owned detail record. (`packages/web/src/play/use-draft-session.ts:29`, `packages/web/src/play/use-draft-session.ts:48`, `packages/web/src/play/draft-restore.ts:23`, `packages/web/src/play/use-draft-uploads.ts:209`.) |
| Browser draft/UI state | Shell-level `PlayDraftProvider` owns session refs, current document, acknowledged server view, edit counters, frozen pending save, statuses, reveal requests, save timer, review owner and upload controllers. Play route owns touched fields/focus refs. Only the active draft ID is written to localStorage; unsaved bytes/values are not browser-persisted. Prompt drafts remain memory-only in their existing context. (`packages/web/src/play/draft-context.tsx:55`, `packages/web/src/play/use-draft-session.ts:27`, `packages/web/src/play/draft-save.ts:45`, `packages/web/src/play/review-state.ts:60`, `packages/web/src/play/use-draft-uploads.ts:38`, `packages/web/src/routes/play.tsx:54`, `packages/web/src/play/draft-restore.ts:6`, `packages/web/src/lib/form-drafts.tsx:16`.) |
| Tutorial cursor | SQLite settings owns persisted version/session/mutation hash; `useTutorialSession` owns current React session/error and in-memory queue, pending request, restore gates. (`packages/app/src/slices/settings/tutorial.ts:25`, `packages/web/src/tutorial/use-session.ts:17`.) |
| Schedule definitions and occurrence history | SQLite `schedules` stores cadence/timezone/policies/pinned template/version and next occurrence; `schedule_runs` stores claim/result history. The in-process runner holds only a `ticking` exclusion flag. (`packages/app/src/kernel/db/migrations/0009-scheduled-jobs.sql:1`, `packages/app/src/slices/schedules/scheduler.ts:25`.) |
| Portable backup and diagnostics | Export/import ZIP bytes and diagnostics JSON exist only for the request lifetime; imported resources persist in SQLite/staging after each write. Storage usage is calculated from current filesystem state without a cache. (`packages/app/src/slices/storage/portable.ts:85`, `packages/app/src/slices/storage/portable.ts:135`, `packages/app/src/slices/storage/portable.ts:234`, `packages/app/src/edge/http/diagnostics.ts:10`.) |

## Side-effect boundaries

- Boot supplies clock, IDs, filesystem paths, SQLite, provider adapters, subprocess/HTTP dependencies and event emission to slices; the runner kernel invokes injected stage functions. Recipe planning and draft conversion calculate values before persistence/provider execution. (`packages/app/src/main.ts:345`, `packages/app/src/slices/rebuild/recipe-save.ts:1`, `packages/web/src/play/state.ts:137`.)
- `transact` owns synchronous nested SQLite savepoints and explicitly rejects asynchronous transaction callbacks. Asset preparation allocates a unique directory, writes/seals/fsyncs bytes before registration; Save/publication transactions register descriptors and references, then cleanup removes only unregistered prepared assets. Database atomicity does not include filesystem operations or event delivery. (`packages/app/src/kernel/db/tx.ts:9`, `packages/app/src/slices/storage/assets.ts:30`, `packages/app/src/slices/storage/assets.ts:43`, `packages/app/src/slices/revisions/mutations.ts:88`, `packages/app/src/slices/rebuild/runtime-publication.ts:99`.)
- Provider queue/attempt wrappers own admission, deadlines and retry accounting around adapter network/CLI work. FFmpeg/alignment are local execution seams; their artifacts become revision publication inputs. Catalogue refresh and telemetry are separate outbound HTTP paths; update checks/install use injected updater effects. (`packages/app/src/kernel/runner/providers.ts:105`, `packages/app/src/kernel/runner/attempt.ts:60`, `packages/app/src/slices/rebuild/runtime-export.ts:1`, `packages/app/src/slices/rebuild/runtime-subtitles.ts:1`, `packages/app/src/catalog/store.ts:54`, `packages/app/src/slices/telemetry/collector-client.ts:40`, `packages/app/src/updater/service.ts:55`.)

- Composition supplies draft DB, paths, IDs/UUID, clock/log, catalogue, font resolver, readiness/model access, runner, global event emitter and telemetry callback. Draft CRUD and review do not invoke pipeline work; explicit Start reaches existing admission. (`packages/app/src/main.ts:236`, `packages/app/src/slices/play-drafts/service.ts:155`, `packages/app/src/slices/play-drafts/review.ts:49`, `packages/app/src/slices/play-drafts/start.ts:109`.)
- Review reads attachment stats and font bytes, resolved templates/settings and local catalogue; Start awaits provider/model readiness outside the final savepoint and repeats local checks inside it. (`packages/app/src/slices/play-drafts/service.ts:79`, `packages/app/src/slices/play-drafts/review-inputs.ts:68`, `packages/app/src/slices/play-drafts/review-inputs.ts:142`, `packages/app/src/slices/play-drafts/readiness.ts:43`, `packages/app/src/slices/play-drafts/start.ts:95`.)
- SQLite admission/receipt/ownership is atomic; asset copy/text writes are synchronous disk effects within nested savepoint callbacks and cannot be rolled back by SQLite. Copies preserve staging originals on rollback; reconciliation collects project orphans. Post-commit telemetry, cleanup and dispatch are independent effects. (`packages/app/src/slices/storage/staging.ts:206`, `packages/app/src/slices/storage/staging.ts:269`, `packages/app/src/slices/admission/start.ts:64`, `packages/app/src/slices/play-drafts/start.ts:123`, `packages/app/src/slices/storage/reconcile.ts:47`.)

## Failure paths

Preparation uses bounded answer-validation retries; malformed or unrenderable cues submit no TTS. Preparation-only rebuilds retain the selected TTS cap. Recovery reuses completed cues/audio and requires review for uncertain submissions; late article/override edits remain origin-only. Failed text-file publication removes pending files and keeps earlier audio (`packages/app/src/slices/rebuild/runtime-provider.ts:282`, `packages/app/src/slices/rebuild/preview-plan.ts:226`, `packages/app/test/revision-preparation-restart.test.ts:13`, `packages/app/src/slices/rebuild/runtime-narration-text.test.ts`).

- **Save/create/fork/restore transport and conflicts:** API JSON parsing and unexpected response shapes throw; expected 400/404/409 problem bodies become typed replies. Session drain catches network/schema exceptions into error state and retains the frozen pending request; its `finally` releases the running guard. Conflict does not install server values. Restore checks selection/generation before applying; fork keeps its stable attempt across exceptions, catches into error state and clears only its running guard. Discard propagates errors to its caller while `finally` releases the discard guard, retaining input until confirmed deletion. Timer teardown cancels the debounce and suppresses React renders, without a final autosave guarantee on browser shutdown. (`packages/web/src/play/draft-api.ts:66`, `packages/web/src/play/use-draft-session.ts:57`, `packages/web/src/play/use-draft-session.ts:124`, `packages/web/src/play/use-draft-session.ts:137`, `packages/web/src/play/use-draft-session.ts:203`, `packages/web/src/play/use-draft-session.ts:228`, `packages/web/src/play/use-draft-session.ts:288`, `packages/web/src/play/use-draft-session.ts:335`.) Browser storage exceptions only warn; independent choices-refresh failures warn through allSettled. (`packages/web/src/play/draft-restore.ts:7`, `packages/web/src/play/draft-restore.ts:23`.)
- **Invalid/corrupt data:** stored draft JSON syntax or unsupported schema gives invalid-draft without deletion; missing or wrong-size attachments appear reattach. Other stat/database exceptions propagate from read/CRUD without service-local catch to HTTP `onError`. Save/discard database writes commit before reference cleanup; cleanup skips shared/copying/transaction-bound files and logs disk-removal failure, but uncaught DB/log failures can still turn a committed operation into an HTTP failure. (`packages/app/src/slices/play-drafts/service.ts:66`, `packages/app/src/slices/play-drafts/service.ts:79`, `packages/app/src/slices/play-drafts/service.ts:206`, `packages/app/src/slices/play-drafts/service.ts:281`, `packages/app/src/slices/storage/staging-refs.ts:14`, `packages/app/src/edge/http/app.ts:142`, `packages/app/src/edge/http/problem.ts:43`.)
- **Multipart/disconnect/copy failure:** malformed headers produce 415/400; parser construction catches and wraps 400; parser/source/file failures reject the envelope and destroy all streams. Request abort follows that path; `finally` removes its abort listener. The staged pipeline catches failures, waits for sink close, detaches referencing attachments into reattach, tries disk cleanup, emits/logs and rethrows. Cleanup disk failure leaves a staging row for boot; progress-emitter failure logs, and logger failure is not locally caught. Empty-file refusal also discards staging. Allocation, final metadata writes and upload-service infrastructure failures otherwise propagate. (`packages/app/src/edge/http/multipart.ts:10`, `packages/app/src/edge/http/multipart.ts:24`, `packages/app/src/edge/http/multipart.ts:44`, `packages/app/src/edge/http/multipart.ts:78`, `packages/app/src/edge/http/multipart.ts:91`, `packages/app/src/slices/storage/staging.ts:126`, `packages/app/src/slices/storage/staging.ts:158`, `packages/app/src/slices/storage/staging.ts:177`, `packages/app/src/slices/storage/staging.ts:301`.) Browser removal/replacement aborts no-longer-owned requests; provider teardown aborts remaining attachment operations. Catch installs reattach only for current ownership; finally deletes the operation. Font errors use matching operation/generation/lifetime checks and finally release the font operation. (`packages/web/src/play/use-draft-uploads.ts:57`, `packages/web/src/play/use-draft-uploads.ts:67`, `packages/web/src/play/use-draft-uploads.ts:164`, `packages/web/src/play/use-draft-uploads.ts:216`.) Draft preview uses a stream without route-local asynchronous stream-error recovery (`packages/app/src/edge/http/draft-files.ts:74`).
- **Review/Start failure:** missing-font exceptions become field refusals; other resolver/readiness/DB exceptions propagate to HTTP. Expected stale/readiness refusals release the matching Start claim, clearing the review only for stale identity. Uncaught errors after the initial claim leave durable starting state; retry uses the same identity. Final-transaction rollback creates no committed receipt/projects, although copied orphan bytes can remain. Each post-commit telemetry/release/dispatch action catches and logs independently, with a second catch swallowing logger failure; receipt success survives any such failure, and replay does not repeat those actions. (`packages/app/src/slices/play-drafts/review.ts:30`, `packages/app/src/slices/play-drafts/start.ts:69`, `packages/app/src/slices/play-drafts/start.ts:91`, `packages/app/src/slices/play-drafts/start.ts:119`, `packages/app/src/slices/play-drafts/start.ts:123`, `packages/app/src/slices/play-drafts/start.ts:133`, `packages/app/src/slices/play-drafts/start-repo.ts:146`.) Browser review catch/finally reports error/releases pending; Start catch marks uncertain and preserves identity, finally releases the running flag. Recovery reads the receipt before retrying the same Start; restore reinstates a durable pending identity. (`packages/web/src/play/review-state.ts:97`, `packages/web/src/play/review-state.ts:128`, `packages/web/src/play/review-state.ts:184`, `packages/web/src/play/review-state.ts:212`, `packages/web/src/play/review-state.ts:230`.) Draft API does not impose a request timeout, and Start/review do not wire HTTP disconnect cancellation to admission (`packages/web/src/play/draft-api.ts:94`, `packages/app/src/edge/http/drafts.ts:113`, `packages/app/src/edge/http/drafts.ts:125`).
- **Tutorial failure/teardown:** stored JSON syntax failure becomes unreadable; unsupported content is preserved and save returns 409 unreadable/conflict. Database failures otherwise reach HTTP. Browser flush, restore and reset each catch into visible error and use finally to release their running/restoring guards; pending write identity survives failed save. Restore generation checks ignore stale work after reset. Tutorial section-reveal cleanup ignores its stale promise completion, while a rejected navigation promise has no local catch. (`packages/app/src/slices/settings/tutorial.ts:38`, `packages/app/src/slices/settings/tutorial.ts:57`, `packages/app/src/edge/http/tutorial.ts:18`, `packages/web/src/tutorial/use-session.ts:55`, `packages/web/src/tutorial/use-session.ts:127`, `packages/web/src/tutorial/use-session.ts:150`, `packages/web/src/tutorial/runner.tsx:86`.)

- **Provider idle timeout, rejection or cancellation:** `attempt` captures the adapter error, disposes the deadline, records canceled/classified outcomes and retries nonterminal failures within its limit using provider Retry-After or backoff. Terminal/exhausted failures propagate to invocation execution, which marks the unfinished piece failed/pending and rethrows to the runner; the runner concludes failed or paused/canceled. Queue cancellation occurs before provider attempt creation for a waiting request. Successful previously published pieces remain separate durable records. (`packages/app/src/kernel/runner/attempt.ts:84`, `packages/app/src/kernel/runner/attempt.ts:104`, `packages/app/src/kernel/runner/attempt.ts:126`, `packages/app/src/kernel/runner/queue.ts:37`, `packages/app/src/slices/rebuild/runtime-run.ts:30`, `packages/app/src/kernel/runner/index.ts:150`.)
- **Save/publication partial failure:** savepoint errors unwind and rethrow; cleanup failure while unwinding throws with the original failure as cause. Save's `finally` removes prepared assets absent from the registered-assets table; publication also runs cleanup in `finally`. Asset deletion failures are logged without replacing the primary write error, so orphan files can survive until reconciliation. A receipt committed before response loss supports exact replay, rather than another mutation. (`packages/app/src/kernel/db/tx.ts:27`, `packages/app/src/kernel/db/tx.ts:36`, `packages/app/src/slices/revisions/mutations.ts:194`, `packages/app/src/slices/rebuild/runtime-publication.ts:114`, `packages/app/src/slices/storage/assets.ts:82`, `packages/app/src/slices/revisions/mutation-request.ts:1`.)
- **Rebuild readiness or notification failure:** readiness exceptions become a logged typed refusal before admission; head/recipe/cost changes reject the stale preview under lock. Runner notification failure after admission is logged while the durable receipt remains replayable. The client keeps request memory across transport exceptions and clears a refused stale preview before requiring another review. (`packages/app/src/slices/rebuild/service.ts:130`, `packages/app/src/slices/rebuild/service.ts:140`, `packages/app/src/slices/rebuild/service.ts:168`, `packages/web/src/project/revision-workspace.tsx:68`, `packages/web/src/project/revision-workspace.tsx:146`.)
- **SSE disconnect and obsolete events:** hub write rejections drop subscribers and log; abort removes subscriptions. Events are not a durable replay log. Browser reconnect refetches persisted data, and current-work/revision visibility filters prevent historical progress from overwriting the current view. JSON parsing of an invalid event has no local catch in the browser listener. (`packages/app/src/edge/events/hub.ts:79`, `packages/app/src/edge/events/hub.ts:103`, `packages/app/src/edge/events/visibility.ts:1`, `packages/web/src/events.ts:98`, `packages/web/src/events.ts:131`.)
- **Missing retained files and stream errors:** absence produces typed missing-file/404; stat/read permission failures are not converted to absence and propagate to the HTTP error boundary. The file route provides a read stream without a route-local asynchronous stream-error recovery handler. Folder-launch failure is caught/logged and returns 503. (`packages/app/src/slices/revisions/downloads.ts:85`, `packages/app/src/edge/http/revision-files.ts:36`, `packages/app/src/edge/http/revision-files.ts:68`, `packages/app/src/edge/http/app.ts:142`.)
- **Catalogue/update/network failure:** invalid local catalogue reads retain the prior parsed value with a warning; refresh failures propagate after reader cancellation/release and do not replace validated current data. Update checks and asynchronous install failures become reported updater error state. (`packages/app/src/catalog/store.ts:38`, `packages/app/src/catalog/store.ts:54`, `packages/app/src/updater/service.ts:64`, `packages/app/src/updater/service.ts:123`.)
- **Portable storage failure:** export catches any read/serialization/ZIP error and returns a generic 500. Import validates ZIP and manifest before its first write, but resource upserts and staged-file writes are sequential rather than one transaction; an exception after earlier writes can leave those earlier resources imported. The route reports invalid/incomplete as 400. (`packages/app/src/edge/http/storage.ts:10`, `packages/app/src/edge/http/storage.ts:31`, `packages/app/src/slices/storage/portable.ts:135`.)
- **Schedule failure:** one in-process exclusion flag prevents overlapping scheduler ticks. Each due claim advances the schedule and inserts its run transactionally before asynchronous draft Review/Start; any later expected dispatch failure records a safe reason, while an unexpected error records `The scheduled run failed.` Boot turns abandoned `running` rows into failed history. (`packages/app/src/slices/schedules/scheduler.ts:25`, `packages/app/src/slices/schedules/scheduler.ts:43`, `packages/app/src/slices/schedules/scheduler.ts:96`, `packages/app/src/slices/schedules/repo.ts:207`.)
- **Update-client recovery:** the browser polls every two seconds while installation/restart recovery is active, reloads only after a different activated version reports idle/error, and abandons the updating presentation after the same two-minute activation window with an explicit restart message. Persisted server status can therefore recover a remounted tab without an in-memory accepted version. (`packages/web/src/updates/api.ts:8`, `packages/web/src/updates/use-update.ts:79`, `packages/web/src/updates/use-update.ts:84`, `packages/web/src/updates/use-update.ts:94`.)
- **Telemetry outage or partial collector commit:** retriable HTTP failures leave events queued; terminal invalid batches are dropped so later events can advance. Collector insertion and aggregate increments are separate statements, so process loss between them can retain the event without its aggregate increment. Shutdown cancels the pending flusher without waiting for collector delivery; an already-running flush can fail to mark delivery after SQLite closes and be resent with the same event IDs. (`packages/app/src/slices/telemetry/collector-client.ts:40`, `packages/app/src/slices/telemetry/flush.ts:43`, `packages/collector/src/index.ts:62`, `packages/app/src/main.ts:303`.)
- **Uncaught infrastructure errors:** routes delegate exceptions to the application problem boundary, which logs a correlation ID and sends a generic 500. Boot closes the database/releases the instance lock and rethrows initialization failures. Batch-timer failures are logged and retried on a later interval; the updater mutation lease is always released. (`packages/app/src/edge/http/app.ts:142`, `packages/app/src/edge/http/problem.ts:41`, `packages/app/src/main.ts:321`, `packages/app/src/main.ts:273`.)

- **Draft list and update gate:** DraftList catches a rejected discard into its visible error and releases busy in `finally`; list query errors expose Retry. Draft/tutorial mutations acquire the existing updater lease; locked updates return409, while acquired leases release in `finally`. (`packages/web/src/play/draft-list.tsx:17`, `packages/web/src/play/draft-list.tsx:25`, `packages/app/src/edge/http/app.ts:120`, `packages/app/src/edge/http/app.ts:138`.)
