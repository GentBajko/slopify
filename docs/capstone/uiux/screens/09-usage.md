---
generated_at_commit: 7bdb84e3f57e
generated_date: '2026-09-25'
capstone_version: 5.2.0
content_hash: cd0e520627ba
paths_covered:
  - :(top)packages/web/src/routes/settings.tsx
  - :(top)packages/web/src/routes/usage.tsx
  - :(top)packages/web/src/components/rail.tsx
---

# Usage

## Mode & job
Read surface for this install's telemetry totals by stage, provider, model and audio segment. It is the last Settings section at `/settings?section=usage`; `/usage` redirects there (`packages/web/src/routes/usage.tsx`, `packages/web/src/router.tsx`).

## Composition
The Usage SectionHead (help behind an InfoTip: this machine only, the same counters feed slopify.stream) leads a tally board and tables; named columns and rows carry the focal data. `UsageBoard` is exported from `routes/usage.tsx` and rendered by `routes/settings.tsx`.

## States
Tally and table skeletons while loading; query errors in a rail; a fresh install shows zero counters with "Numbers appear after your first run."; loaded rows name stage/segment/provider/model and display token, audio, image and thumbnail counters (`packages/web/src/routes/usage.tsx`).

## Motion
No route-specific motion found (`packages/web/src/routes/usage.tsx`).

## Copy
The section is Usage under Settings; table labels are generated by Head, Cell and namedBy (`packages/web/src/routes/usage.tsx`).

## Not in play
Offline, permission-denied and partial-data states are not rendered (`packages/web/src/routes/usage.tsx`).
