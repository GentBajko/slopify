{
  "name": "slopify-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "build": "npm run build --workspace @slopify/web && npm run build --workspace @gentbajko/slopify",
    "deploy": "npm run deploy --workspace @slopify/collector && npm run deploy --workspace @slopify/site",
    "deploy:check": "npm run deploy:check --workspace @slopify/collector && npm run deploy:check --workspace @slopify/site"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
